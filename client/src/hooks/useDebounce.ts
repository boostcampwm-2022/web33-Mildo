// import { useEffect } from 'react';

// const useDebounceApi = (api, delay, deps) => {
//   useEffect(() => {
//     const timer = setTimeout(async () => {
//       const data = await api(deps);
//     }, 500);
//     return () => {
//       clearTimeout(timer);
//     };
//   }, [deps]);
// };

export default {};
